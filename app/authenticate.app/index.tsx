import * as React from 'react';

interface IAuthenticateAppProps {
}

const AuthenticateApp: React.FunctionComponent<IAuthenticateAppProps> = (props) => {
  return <div className=""></div>;
};

export default AuthenticateApp;
